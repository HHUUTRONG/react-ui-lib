module.exports = {
    plugins: {
      tailwindcss: {},
      autoprefixer: {},
    },
    map: {
      inline: false, // Generate separate source map files
      annotation: true, // Add source map annotation comments
    },
  }